spring:
  datasource:
    url: jdbc:mysql://localhost:11112/slam
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: dev
    password: dev

  jpa:
    generate-ddl: true
    database: mysql
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        hbm2ddl:
          query.in_clause_parameter_padding: true
          auto: create

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-name: kakao
            client-id: 529839ee72199bbe32919f0ef14d600f
            client-secret: XUjgULq3hk4MFBLf5lJxs6qHaDqcldcO
            scope:
              - profile_nickname
              - profile_image
              - account_email
            redirect-uri: "http://localhost:8080/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            client-authentication-method: POST
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

#  config:
#    import: classpath:aws.yaml

jwt:
  header: token
  issuer: slams
  client-secret: EENY5W0eegTf1naQB2eDeyCLl5kRS2b8xa5c4qLdS0hmVjtbvo8tOyhPMcAmtPuQ
  expiry-seconds: 604800 # 7 days

app:
  oauth2:
    authorizedRedirectUris:
      - http://localhost:3000/oauth/redirect

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true